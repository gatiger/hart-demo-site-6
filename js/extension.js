// Extension Office page

document.addEventListener("DOMContentLoaded", () => {
  const card = document.querySelector(".comingSoonCard");
  if (!card) return;

  // Placeholder for future functionality
  card.setAttribute("data-ready", "true");
});